// import { registerEditorConfig, registerEditorGroups } from '@io-gui/editors'
// import { ioNumberSlider } from '@io-gui/sliders'
// import { SpotLightShadow } from 'three/webgpu'

// registerEditorConfig(SpotLightShadow, [
//   ['focus', ioNumberSlider({min: 0, max: 1, step: 0.01})],
//   ['aspect', ioNumberSlider({min: 0.1, max: 4, step: 0.01})],
// ])

// registerEditorGroups(SpotLightShadow, {
//   Main: ['focus', 'aspect'],
// })

