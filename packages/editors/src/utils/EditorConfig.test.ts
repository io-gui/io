// import { ioBoolean } from '@io-gui/inputs';
// import { ioSlider } from '@io-gui/sliders';
// import { getEditorConfig, registerEditorConfig } from './EditorConfig';

export default class {
  run() {
    describe('EditorConfig.test', () => {
    //   it('should return default config for an object', () => {
    //     const config = getEditorConfig({
    //       'string': 'string',
    //       'number': 0.01,
    //       'boolean': true,
    //       'object': {},
    //       'null': null,
    //       'undefined': undefined,
    //     });
    //     expect(config).to.eql({
    //       boolean: {name: 'io-switch'},
    //       null: {name: 'io-input-base'},
    //       number: {name: 'io-number', props: {step: 0.01}},
    //       object: {name: 'io-object'},
    //       string: {name: 'io-string'},
    //       undefined: {name: 'io-input-base'},
    //     });
    //   });
    //   it('should register new configs', () => {
    //     registerEditorConfig(Object, [
    //       [Number, ioSlider({min: 0, max: 100, step: 1})],
    //       ['boolean', ioBoolean()],
    //     ]);
    //     const config = getEditorConfig({
    //       'number': 0.01,
    //       'boolean': true,
    //     });
    //     expect(config).to.eql({
    //       number: {name: 'io-slider', props: {min: 0, max: 100, step: 1}},
    //       boolean: {name: 'io-boolean'},
    //     });
    //   });
    })
  }
}