import { IoText } from '../index';

const element = new IoText();
element.style.display = 'none';
document.body.appendChild(element as unknown as HTMLElement);

export default class {
  run() {
  }
}
