{
  "name": "io-gui-workspace",
  "version": "2.0.0-alpha.5",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.5.0",
  "scripts": {
    "dev": "vite",
    "build": "pnpm lint && pnpm clean && tsc -b",
    "build:watch": "tsc -b -w",
    "bundle": "pnpm build && node bundle.js",
    "docs:api": "typedoc",
    "lint": "eslint . --fix",
    "lint:check": "eslint .",
    "test": "vitest run --project unit",
    "coverage": "vitest run --project coverage --coverage",
    "bench": "vitest bench --run --no-file-parallelism --reporter=json --outputFile=./benchmarks/results.json",
    "bench:baseline": "vitest bench --run --no-file-parallelism --reporter=json --outputFile=./benchmarks/results-baseline.json",
    "clean": "rm -rf packages/**/dist packages/**/tsconfig.tsbuildinfo"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@rollup/plugin-strip": "^3.0.4",
    "@stylistic/eslint-plugin": "^5.10.0",
    "@types/node": "^25.9.3",
    "@vitest/browser-playwright": "5.0.0-beta.5",
    "@vitest/coverage-v8": "5.0.0-beta.5",
    "@vitest/ui": "5.0.0-beta.5",
    "eslint": "^10.5.0",
    "playwright": "^1.60.0",
    "terser": "^5.48.0",
    "typedoc": "^0.28.19",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.61.0",
    "vite": "^8.0.16",
    "vitest": "5.0.0-beta.5"
  }
}
