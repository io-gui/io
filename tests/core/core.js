import {IoCore} from "../../src/io.js";

export default class {
  constructor() {
  }
  run() {
    describe('IoCore: ', () => {
      it('IoCore', () => {
        chai.expect('TODO').to.not.exist;
      });
    });
  }
}
