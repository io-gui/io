import {IoStorage} from "../../build/io.min.js";

export default class {
  constructor() {
  }
  run() {
    describe('IoStorage: ', () => {
      it('IoStorage', () => {
        // chai.expect('TODO').to.not.exist;
      });
    });
  }
}
