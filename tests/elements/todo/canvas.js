import {IoCanvas} from "../../build/io.js";

export default class {
  constructor() {
  }
  run() {
    describe('IoCanvas: ', () => {
      it('IoCanvas', () => {
        // chai.expect('TODO').to.not.exist;
      });
    });
  }
}
