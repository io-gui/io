import {IoSelector} from "../../build/io.js";

export default class {
  constructor() {
  }
  run() {
    describe('IoSelector: ', () => {
      it('IoSelector', () => {
        // chai.expect('TODO').to.not.exist;
      });
    });
  }
}
