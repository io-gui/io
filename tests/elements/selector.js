import {IoSelector} from "../../src/io.js";

export default class {
  constructor() {
  }
  run() {
    describe('IoSelector: ', () => {
      it('IoSelector', () => {
        // chai.expect('TODO').to.not.exist;
      });
    });
  }
}
