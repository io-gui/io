import {IoArray} from "../../build/io.min.js";

export default class {
  constructor() {
  }
  run() {
    describe('IoArray', () => {
      it('IoArray', () => {
        // chai.expect('TODO').to.not.exist;
      });
    });
  }
}
