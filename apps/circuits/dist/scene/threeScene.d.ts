import { BoxGeometry, CapsuleGeometry, Group, InstancedMesh, Mesh, Object3D, PerspectiveCamera, SphereGeometry, Vector2, Vector3 } from 'three/webgpu';
import { ThreeApplet, ThreeAppletProps } from '@io-gui/three';
import { Line } from '../game/items/line';
import { Pads } from '../game/pads';
import { Grid } from './grid';
import { DebugPlanes } from './debugPlanes.js';
import { PadsStateMaterial } from './materials/PadsStateMaterial.js';
import { TerminalsStateMaterial } from './materials/TerminalsStateMaterial.js';
import { Layer0LinesStateMaterial } from './materials/Layer0LinesStateMaterial.js';
import { Layer1LinesStateMaterial } from './materials/Layer1LinesStateMaterial.js';
import { Game } from '../game/game';
type ThreeSceneProps = ThreeAppletProps & {
    game: Game;
};
export declare class ThreeScene extends ThreeApplet {
    game: Game;
    camera: PerspectiveCamera;
    cameraRig: Group;
    cameraTarget: Object3D;
    grid: Grid;
    debugPlanes: DebugPlanes;
    pads: InstancedMesh;
    terminals: InstancedMesh;
    layer0Lines: InstancedMesh;
    layer1Lines: InstancedMesh;
    padsMaterial: PadsStateMaterial;
    terminalsMaterial: TerminalsStateMaterial;
    layer0LinesMaterial: Layer0LinesStateMaterial;
    layer1LinesMaterial: Layer1LinesStateMaterial;
    hitMarker: Mesh;
    oldHitMarker: Mesh;
    rawHitMarker: Mesh;
    static padGeometry: SphereGeometry;
    static terminalGeometry: BoxGeometry;
    static lineGeometry: CapsuleGeometry;
    _drag: Vector3;
    static get Listeners(): {
        'game-update': string;
        'game-init': string;
    };
    constructor(args: ThreeSceneProps);
    ready(): void;
    onGameInit(): void;
    onGameUpdate(): void;
    updateMarkers(hit: Vector2, oldHit: Vector2, rawHit: Vector2): void;
    updateDrag(screen: Vector2, screenStart: Vector2): void;
    initGrid(game: Game): void;
    updateGrid(game: Game): void;
    updatePads(pads: Pads): void;
    updateTerminals(pads: Pads): void;
    updateLines(layer0Lines: Line[], layer1Lines: Line[]): void;
    private _updateLayerLines;
    private _coordToTextureU;
    private _coordToTextureV;
    private _ensureMeshCapacity;
    onAnimate(delta: number, time: number): void;
    dispose(): void;
}
export {};
//# sourceMappingURL=threeScene.d.ts.map